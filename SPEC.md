# DroneRadar 仕様書

ドローン関連情報を一画面に集約するダッシュボード。macOS / Windows / Android / iOS / Fire で動作し、
外部ライブラリと API キーを一切使わない。

- 最終更新: 2026-08-19
- 収集ソース: 106本 / 分類: 17ジャンル（7グループ）
- コード規模: Python 2,946行 + JavaScript 3,801行（UI 2,683 / 共通 1,118）+ CSS 1,145行

---

## 1. 設計の前提

制約は「無料の範囲で」の一点から出ている。これが全ての設計判断を規定した。

| 前提 | 帰結 |
|---|---|
| API キーを使わない | X は Yahoo!リアルタイム検索の `__NEXT_DATA__`、YouTube は検索結果の `ytInitialData` から取る |
| 外部ライブラリを使わない | Python は標準ライブラリのみ。フロントもフレームワークなし |
| 常に最新を表示 | 15分ごとに自動収集。SNS は60秒、株価は5分 |
| 一画面に収める | スクロールしない前提でレイアウトを組む |
| ローカルで完結 | 既定で 127.0.0.1 のみ。外部にデータを送らない |

## 2. 全体構成

```
[収集]  Python                    [表示]  ブラウザ
  scrapers ── RSS/HTML/JSON          js/api ──┐
      ↓                                       │
  collector ── 正規化・分類・地理判定 ──→ items.json ──→ 各描画モジュール
      ↓                                       │
  translate ── 日本語訳（キャッシュ付）       └── 15の ES モジュール
      ↓
  server ── HTTP + 静的配信
```

サーバは 127.0.0.1:8783 に立ち、`.app`（または `.bat`）がそれを起動して
Chromium のアプリモードでウィンドウを開く。UI は HTML/CSS/JS なので、
実体は「ローカル Web アプリを OS のアプリで包んだもの」。

## 3. Python モジュール

| モジュール | 行数 | 責務 |
|---|---:|---|
| `collector.py` | 847 | 収集の統括。正規化、ジャンル判定、重複検出、保存 |
| `scrapers.py` | 483 | ソース種別ごとの取得。RSS/Atom/RDF、HTML、arXiv、GitHub、HN、YouTube、Yahoo!リアルタイム |
| `server.py` | 482 | HTTP。API 10本と静的配信、背景ループ3本 |
| `sources.py` | 508 | ソース定義（106本）とカテゴリ定義（17種）。純粋なデータ |
| `net.py` | 332 | HTTP 取得、フィード解析、日付解析、画像取得 |
| `geo.py` | 306 | 47都道府県と約75カ国の辞書、地名判定、重要度スコア |
| `translate.py` | 87 | 翻訳とキャッシュ |

### ソースの種別

`type` フィールドで取得方法が決まる。

| type | 取得方法 |
|---|---|
| `rss` | RSS 2.0 / Atom / RDF。壊れた XML の修復あり |
| `dronejournal` | Impress のドローンジャーナル専用。RSS が無いため HTML を解析 |
| `arxiv` | arXiv API |
| `github` | GitHub 検索 API（未認証） |
| `hn` | Hacker News Algolia API |
| `youtube_search` | 検索結果ページの `ytInitialData` |
| `yahoo_realtime` | Yahoo!リアルタイム検索の `__NEXT_DATA__`（X の投稿） |
| `mastodon` | Mastodon の公開タイムライン API |

`strict: true` を付けたソースは、汎用媒体（PR TIMES、日経クロステック、
The War Zone など）としてドローン関連語での絞り込みを通す。

## 4. データ

### item

収集した1記事。`data/items.json` に配列で保存（最大5000件、45日で失効）。

| フィールド | 型 | 内容 |
|---|---|---|
| `id` | str | URL の SHA-1 先頭16桁 |
| `title` / `title_ja` | str | 原題 / 日本語訳 |
| `summary` / `summary_ja` | str | 要約 / 日本語訳 |
| `url` / `image` | str | 記事 URL / 画像 URL |
| `published` / `fetched` | int | UNIX 秒 |
| `source` / `source_id` / `publisher` | str | ソース名 / ID / 媒体名 |
| `category` | str | 17ジャンルのいずれか |
| `lang` | str | `ja` / `en` |
| `prefectures` / `countries` | list | 判定した都道府県コード / 国コード |
| `scope` | str | `national` / `local` / `overseas` |
| `flagship` / `importance` | int | 重要度スコア |
| `cluster` / `related` | int / list | 同一話題の媒体数と相手の id |

### ジャンル（17分類 / 7グループ）

分類は 17 のまま、色は 7 グループが持つ。17 色は地図の塗りや直径 3px の
レーダー輝点では見分けられず、凡例が読む価値を失っていたため。グループ内の
区別はアイコンが担う。グループ定義は `web/js/genre.js`。

| グループ | 色 | 含む分類 |
|---|---|---|
| ニュース | `#4cc4ff` | 国内ニュース、海外ニュース |
| 防衛・安全 | `#ff6b7d` | 防衛・軍事、対ドローン・安全 |
| 規制・制度 | `#ffb445` | 規制・制度 |
| 現場・運用 | `#5fdc86` | 産業活用、測量・点検、災害・救助、eVTOL・空飛ぶクルマ |
| 製品・技術 | `#ac92ff` | 製品・新機種、研究・論文、開発・OSS |
| ビジネス | `#ff9366` | ビジネス・投資 |
| 空撮・催事 | `#f77fc0` | 空撮・FPV、イベント・大会、コミュニティ、動画 |

絞り込みの粒度は落としていない。サイドバーはグループ7行を出し、開くと
その下に分類が並ぶ。`S.filter.group` だけならグループ全体、`S.filter.cat`
があればその分類だけを表示する。

ジャンルはソース定義が持つ値を起点に、本文から再判定する。判定順序が重要で、
上から順に最初に当たったものを採る。

### 拒まれるソースの予備経路

5社のフィードは、この Mac には答えて毎時の収集機には答えない。Cloudflare は
**アドレスで**データセンターを弾くので、UA をどう変えても通らない。同じ記事を
Google News は索引していて誰にでも答えるため、ソースに `fallback` を持たせ、
**直リンクが失敗したときだけ**そちらへ回す。

置き換えではなく予備。直リンクが通る機械では直リンクが使われる（速く、
内容も濃い）。検索経由で取れた回は `via="search"` を残す——直リンクが
死んでいるのに緑の正常表示になるのは、知らないままでいたくない類の嘘なので。

Reddit の3本は Google News が Reddit を索引していないため回復できない。

**`merge_builtins` の項目一覧に `fallback` を入れること。** 保存済みの
定義は決め打ちの一覧の項目しか更新しないので、後から加えた項目は
保存側に届かない。実際これを忘れて一度公開し、コードは正しいのに
データが一世代前で不調件数が動かなかった。同じ穴は core/collector.js の
`OWNED` にもある。

### ソースの穴（第3次追加で埋めたもの）

87本の時点で欠けていたのは細かい隅ではなく、主題そのものだった。

| 欠けていた領域 | 追加 |
|---|---|
| 国家資格・機体認証 | 日本の運用者全員が通る制度が1本も無かった |
| 電波法・無線局 | 航空法の側は拾えていたが、電波の側が無かった |
| 事故・墜落・書類送検 | 安全情報が皆無 |
| 農業ドローン | 混成クエリ1本の一部だけ |
| 自治体の実証実験 | 日本の実用化の主戦場 |
| 水中・港湾 | 無し |
| JUIDA・官民協議会 | 無し |
| Commercial UAV News / Unmanned Systems Technology | 業界大手2誌。RSS が停止している |
| Skydio / Zipline / Wing / Anduril | DJI 規制の受け皿になっている米メーカー |
| BVLOS・回廊・Part 108 | 制度運用の核心 |
| 空港での妨害 | ニュース価値が高い |

フィードが停止・空のサイト（Commercial UAV News、Unmanned Systems
Technology、JUIDA、Skydio）は Google News の `site:` または名前検索で拾う。
MLIT と FAA に既に使っていた手と同じ。

アイコンの出る場所は、サイドバーのグループ行、記事カード／タイルのメタ行、
ジャンル列の見出し、ジャンル送りのチップ、地図キャプション、地図のピン。

地図のピンだけは SVG 単位ではなく画面 px（26px）で大きさを決めている。
2枚の地図は viewBox の尺度が大きく違い、右列の幅も画面で変わるため、
SVG 単位で指定すると日本地図側が 5px になってしまう。

地図のバブルには常時アイコンを置かない。1つの地域に複数ジャンルの記事が
あっても最多の1つしか出せず、地域の性格を実際より断定してしまう。
スポットライト（ピン）は対象が1記事に確定しているので、そこでだけ出す。

### 国旗

国コードは ISO-3166 alpha-2 なので、国旗は表を持たず符号から導く
（`flagOf`: 各文字を Regional Indicator に写す）。Windows には国旗の
グリフが無く2文字のまま表示されるが、それでも読めるラベルではある。

1. `aam` — 空飛ぶクルマ、eVTOL、エアタクシー
2. `defense` — 軍事語。「迎撃」を含む戦争報道を対ドローンに取られないよう先に置く
3. `security` — 対ドローン、侵入、無許可飛行、書類送検
4. `disaster` — 災害、救助、捜索、消防
5. `events` — 開催、展示会、レース、ショー
6. `survey` — 測量、点検、インフラ、点群
7. `industry` — 配送、農薬散布、国家資格

再判定の対象は `jp_news` / `world_news` / `business` / `hobby` のみ。
`research` や `dev` は既に内容を言い当てているので触らない。

## 5. HTTP API

すべて 127.0.0.1 のみ。認証なし。

| メソッド | パス | 内容 |
|---|---|---|
| GET | `/api/state` | 記事、ソース、カテゴリ、設定の一括取得 |
| GET | `/api/status` | 収集の進行状況とログ |
| GET | `/api/social` | SNS 投稿 |
| GET | `/api/stocks` | 株価 |
| GET | `/img?u=` | 画像のプロキシ（キャッシュ付） |
| POST | `/api/refresh` | 収集を今すぐ実行 |
| POST | `/api/sources/add` | ソース追加。RSS 自動検出 → www 補完 → Google News の `site:` 検索の順に試す |
| POST | `/api/sources/update` | 有効・無効の切り替え |
| POST | `/api/keyword/add` | 検索キーワードの追加 |
| POST | `/api/bookmark` | 保存 |
| POST | `/api/config` | 設定変更 |

静的ファイルは `os.path.realpath` で解決してから公開フォルダ配下か検証する。
正規化前に検証すると `/../../etc/passwd` が通る。

## 6. フロントエンド（19モジュール）

| モジュール | 行数 | 責務 |
|---|---:|---|
| `state.js` | 69 | 共有状態 `S`、DOM ヘルパ、フック表。依存なし |
| `util.js` | 243 | 整形、重複判定、絞り込み |
| `api.js` | 84 | サーバとの通信、定期読み込み |
| `main.js` | 222 | 全体描画、イベント配線、起動、タイマー |
| `cards.js` | 413 | 記事タイル・カード・ジャンル列 |
| `maps.js` | 334 | 日本地図・世界地図とスポットライト |
| `radar.js` | 329 | ライブレーダー |
| `calendar.js` | 317 | イベント暦（月表示＋予定のページ送り） |
| `player.js` | 234 | YouTube 連続再生 |
| `remote.js` | 186 | 十字キー・決定・戻るによるフォーカス移動 |
| `ledboard.js` | 172 | 電光掲示板 |
| `ticker.js` | 123 | SNS 実況 |
| `stocks.js` | 99 | 株価 |
| `sources.js` | 93 | ソース管理画面 |
| `sidebar.js` | 103 | サイドバー（グループ＋分類の2段） |
| `genre.js` | 102 | ジャンル7グループ、色、アイコン、国旗 |
| `rising.js` | 168 | 急上昇（24時間 vs 直近6日の比率） |
| `globe.js` | 289 | 世界地図の球面投影・自転・正面回頭 |
| `sound.js` | 53 | 新着音 |

### 依存の向き

```
state ← util ← 各機能モジュール ← main
```

`state.js` は何も import しない。`main.js` だけが全モジュールを import する。

色の解決だけは例外的な形になっている。`catColor()` は `state.js` にあるが、
どの分類がどのグループかを知るのは `genre.js` である。`state.js` が
`genre.js` を import すると葉でなくなるため、`main.js` が起動時に
`setGroupResolver(groupOf)` で注入する。

葉のモジュールが全体再描画を必要とする場合、`main` を import すると
循環するため、`state.js` のフック表を経由する。

```js
// state.js
const hooks = { renderAll: () => {}, renderCards: () => {}, announceNew: () => {} };
// 関数を持つモジュールが自分で登録する
hooks.renderCards = (items) => renderCards(items);
```

`api → cards → api` の循環は実際に起きた。`api.js` が `announceNew` を
直接 import していたためで、フック経由に変えて解消した。

## 7. ビルドと配布

| スクリプト | 出力 |
|---|---|
| `tools_install.sh` | `/Applications/DroneRadar.app` に導入。起動中なら再起動する |
| `tools_package.sh` | macOS 配布用 zip（約840KB） |
| `tools_package_win.sh` | Windows 配布用 zip（約11MB、Python 同梱） |
| `tools_mkicon.py` | `.icns` と `.ico` を生成（外部ライブラリなし） |
| `tools_mkmap.py` / `tools_mkworld.py` | Natural Earth から地図 SVG を生成 |

macOS 版は署名がないため初回は右クリック起動が要る。Windows 版は
埋め込み版 Python 3.12 を同梱するのでインストール作業が不要。

### 別端末での表示

`DRONERADAR_LAN=1` で 0.0.0.0 に待ち受け、同一 Wi-Fi の端末から見られる。
既定で閉じているのは、閲覧だけでなく設定変更の API も開くため。

## 8. 4プラットフォーム構成

同じ画面と同じデータを、2つの収集エンジンで動かす。

```
                 共通（1文字も変えずに使う）
   ┌──────────────────────────────────────┐
   │  web/    UI 15モジュール・CSS・地図・音   356 KB │
   │  tables/ ソース87本・語彙367語・地名辞書   48 KB │
   └──────────────────────────────────────┘
         ↑                              ↑
   Python 収集エンジン            JavaScript 収集エンジン
   （デスクトップ）                  （モバイル）
   macOS / Windows                Android / iOS / Fire
```

### 収集エンジンは2実装

| | デスクトップ | モバイル |
|---|---|---|
| 言語 | Python 2,946行 | JavaScript 1,118行 |
| 動かし方 | 127.0.0.1 にサーバを立てる | サーバ無し。アプリ内で直接収集 |
| 保存先 | JSON ファイル | 差し替え可能（`store.js`） |
| CORS | 無関係（サーバが取りに行く） | Capacitor が fetch をネイティブ実装に差し替えて回避 |
| 定期収集 | 15分ごとのタイマー | 前面復帰時（iOS は常駐できない） |

サーバ 462行はモバイルでは丸ごと不要になる。

### 共通部品（`core/`）

| モジュール | 行数 | Python との照合 |
|---|---:|---|
| `tables.js` | 27 | — |
| `classify.js` | 52 | ジャンル判定 **2,404件 100%** |
| `geo.js` | 112 | 地理判定 **2,423件 100%** |
| `feed.js` | 200 | RSS/Atom/RDF **392件 100%** |
| `net.js` | 51 | — |
| `scrapers.js` | 201 | 4取得元 **105件 100%** |
| `normalise.js` | 156 | 記事の正規化 **800件 100%** |
| `cluster.js` | 78 | 重複検出 **900件 100%** |
| `translate.js` | 79 | — |
| `collector.js` | 129 | — |
| `store.js` | 33 | — |
| 合計 | **1,118行 / 44 KB** | |

移植で判断が要った点を3つ。

**記事IDの採番を変えた。** ブラウザの `crypto.subtle` は非同期で、SHA-1 をそのまま使うと呼び出し側すべてが async に汚染される。IDは1台の収集内で安定して衝突しなければよいので、同期の FNV-1a 系に置き換えた。

**切り詰めは文字単位。** Python は文字数、JavaScript の `slice` は UTF-16 単位で数える。絵文字1個で2ずれる上、サロゲートペアを半分で切ると文字化けする。

**実体参照はセミコロン省略に対応。** 長いURLが途中で切れて `&amp` で終わることがあり、Python の `html.unescape` はこれを解釈する。

### ビルド

`tools_build_all.sh` が4OS分を通しで作る。

| 対象 | 配布物 | 実機での占有 |
|---|---:|---:|
| macOS | **0.68 MB** | 1.03 MB |
| Windows | **10.73 MB** | 約 24 MB |
| Android / Fire | **3.22 MB** | 約 7 MB |
| iOS | **0.75 MB** | 1.90 MB |

APK 3.2MB の内訳は Capacitor と Android のランタイムが 79.8%、アプリ本体（web+core+tables）は 179 KB で 4.4%。機能を足してもサイズはほとんど動かない。

Electron を採れば macOS が 0.68MB から150MB前後になっていた。WebView を OS から借りる構成がこの差を生んでいる。

### 動画が最後で止まる

停滞監視が**再生終了（state 0）を「正常」として扱っていた**。埋め込みは
自分のプレイリストを勝手に進むから、という理屈で——実際そのとおりだが、
**最後の1本まで**の話。そこで YouTube は停止し、リプレイボタンと関連動画の
一覧を出す。誰も次を促さないので、枠は 4:21 / 4:21 のまま固まる。

終了が2.5秒続いたら次の一巡を取りに行く（プレイリストが先頭に戻った
ときと同じ処理）。一時停止は従来どおり放置する——あれは読み手の意思。

### 電光掲示板の鮮度

並び順が `importance + flagship + 媒体数×8` だけで、**時計が入っていな
かった**。点数の高い記事は窓（24時間）の間ずっと上位に居座り、掲示板は
60候補を12件ずつ3分ごとに回すので、**15分ごとに同じ見出しが戻ってくる**。
「昨日見たのがまだ回っている」はこれ。

- 点数を**6時間で半減**させる（大きい記事は半日残り、並のものは夕方には
  新着に抜かれる）
- 窓を 24時間→**12時間**（足りなければ24時間、それでも足りなければ3日）
- 候補プールを 60→**36**（浅くすると、載るのが「今日のニュースの上位」に
  なる。60件の上位ではなく）

| | 変更前 | 変更後 |
|---|---|---|
| 掲示板の記事の古さ | 10〜17時間前 | **平均4.6時間・最古11時間** |

### 記事ペイン（Yahoo!ニュース準拠）

文字を大きくしても読みにくさが残った。Yahoo!ニュースを実測して原因が
分かった——**効いていたのは文字サイズではなく幅**だった。

| | Yahoo!ニュース | 旧 | 新 |
|---|---|---|---|
| 1件の幅 | **672px** | 300px（2列） | **625px（1列）** |
| 見出し | 16px / 行間1.3 / **太さ400** | 15.5px / 1.42 / 太さ600 | **16px / 1.34 / 太さ400** |
| 行数 | 1〜2行 | 3行 | **2行** |
| サムネ | 61×61 正方形 | 46×29 (16:9) | **56×56 正方形** |
| メタ | 12px | 12.5px | 12px |

672pxなら16pxで約38字入り見出しが1行に収まる。300pxでは17字で3行に
折り返す。**太さ400**なのも意図的で、日本語は小さい字を太らせると画数が
潰れて、強調で得るより可読性で失う方が大きい。

国旗タグは見出しからメタ行へ移した。「🇺🇦 ウクライナ BlueBird Tech、…」
だと見出しの1行目が国名で埋まる。どこの話かは、誰がいつ書いたかと同じ列。

送りの間隔：ジャンル自動送り 8秒→**24秒**、主要ニュース横送り 11秒→20秒。
12本の見出しに8秒は1本0.7秒で、2行目を読んでいる間に入れ替わる。
マウスを乗せれば止まるが、**止め方を知っている必要がある**。既定値だけで
読めることが先。

### キャプションの高さと行数

キャプションは「地図の下に余った分だけ」の可変高だった。帯を固定高に
してからは、この前提で書かれた指定が3か所で悪さをした。

**1. 行数が3行固定。** 枠は約96px、うち余白22px・媒体行26pxで、
本文に使えるのは48px。15.5px×1.45の行が2行分の高さに3行を入れる指定
だったので、はみ出した分が枠に切られて**行の途中で文字が切断**された。
省略ではなく描画の失敗に見える。

→ 枠の高さから入る行数を計算して `-webkit-line-clamp` に入れる。
クランプなら切れ目に「…」が付き、行の途中では切れない。

**2. 媒体行が縮んで消えた。** flex 列の既定では縮むので、見出しが2行に
なると媒体行が 22px→13px に潰れ、国旗と国名が切れて見えなくなった。
見出しはクランプで1行譲れるが、この行は1行かゼロかしかない。
→ `flex: none`。

**3. 画像も要約も無い記事は見出しを18〜23pxで組んでいた。** 可変高だった
頃、そうしないと箱が3分の2空いたため。固定高では23pxの2行が入らず、
ただはみ出す。→ 一律15.5px、行数は高さから。

枠は 96〜124px（下限＝余白＋媒体行＋2行、上限＝世界地図が横長で
上に寄るため、放っておくとキャプションがパネルの半分を占める）。

### レーダーの見出しが英語になる

リポジトリ名を**ラベルから完全に除外**した。順位を下げるだけでは足りず、
GitHub からの取得が多い時間帯には結局それしか記事が無く、盤面が
`trackdraw` `the big dro…` `Dragonfly G…` になっていた。

プロジェクト名を名乗るだけでドローンについて何も言っていないうえ、
**翻訳器が唯一手を付けない種類のタイトル**でもある。日本語をいくら
集めても盤面が英語だったのはこれが理由。輝点としては残り、
ツールチップも持つ。

### 都道府県の分からない日本のニュース

日本の記事435件のうち、都道府県が付くのは312件。残り129件は
**どの県にも属さない**——防衛省の概算要求、全国販売の発表、制度そのもの。
点で指せる場所が無い。

そこで**地図全体をピコーンとさせる**。日本地図の巡回に「全国」という
1地域を足し、その順番では：

- 47都道府県すべてが同時に光る（位置で少しずつ遅らせ、波が渡るように）
- ピンは地図の中心から、地図の幅の6割まで広がる
- **カメラは引く。** 県の記事が寄るのと逆向きの同じ動作——主題が地図の
  どの部分よりも大きいので、地図の方が小さくなる。地図の外枠より22%広い
  viewBox（760→927）まで引き、3.2秒保って戻る。県のズームと同じ間合い
- **ピンは滋賀県から出す（半径は地図幅の92%＝699）。** 滋賀からの最遠点は
  北海道の北端で650、つまり地図幅の0.855倍。72%（547）では青森沖で止まり、
  「全国」を名乗るピンから北が漏れていた。少し多めに取って、波が海岸で
  死なずに越える。実測で47県すべてが半径内 「どこでもない」に
  正直な点は無いが、地図の箱の幾何中心は能登沖——海の上——で、恣意的以下
  だった。かといってリングを出さないと、全県点灯だけでは「ピコーン」に
  見えない。滋賀は本州の真ん中で、陸地
- **地図上にジャンルアイコンは置かない。** 点に置く印は「ここ」と言うが、
  この記事が地図全体に出ている理由は「ここ」が無いこと。ジャンルは
  キャプション側のアイコンが示す
- **前の県のズーム予約を取り消す。** 全国は zoomToRegion を呼ばないので、
  取り消す者が居なかった——全県が光っている間に大阪へ寄っていける状態
  だった
- 光るのは**その瞬間だけ（1.1秒）。** 明滅は塗りを置き換えず明るくする
  だけで、残りの6秒は県別の色分けが読める状態に戻る

これで世界地図から日本を完全に外せた（従来は2位・435件を占めていた）。

**「全国」は21都道府県と同格の1地域ではない。** 総当たりは、どれも同じ
種類の場所であるときに正しい。全国は場所ではなく「場所でないもの全部」で、
日本の記事の**29%**を占める。22分の1の順番＝4%では、3割近い区分が
4分に1回しか回らず、機能が切れているように見えた。

そこでシェアに応じて順番に織り込む（29%なら3地域ごとに1回）。上限は
3地域に1回で、地図が全国ばかりになるのを防ぐ。

実測：広島県 → 東京都 → 大阪府 → **全国**（47県同時点灯・ズームなし）。

### 世界地図に日本は要るか

日本は世界地図で **435件・ロシア(438)に次ぐ2位**。すぐ隣に日本地図が
あるのに、世界地図の日本の順番でも日本のニュースが出るのは重複だった。

ただし丸ごと外すと失うものがある。日本の435件のうち都道府県が付くのは
312件で、**残り129件はどの県にも属さない**——防衛省の概算要求、楽天の
提携、全国販売の発表、制度そのもの。日本地図には出しようがない記事なので、
世界地図から外すと**どちらの地図からも消える**。

そこで世界地図の日本の順番は、**都道府県が付いていない記事だけ**にした。
重複は無くなり、失うものも無い。

| | 変更前 | 変更後 |
|---|---:|---:|
| 世界地図の日本（48時間内） | 都道府県付きも含む | **全国レベル 25件のみ** |
| 世界の順位 | 2位 | **5位** |
| 日本地図 | 18地域 | 変更なし |

塗りとバブルは全件のまま。「世界のドローン報道のうち日本がどれだけか」は
それ自体が情報なので、変えるのは順番に出す記事だけ。

### スポットライトのズーム

ピンの後に viewBox を対象地域に寄せる。世界地図で「ズームアウトする」
「アメリカの拡大が足りない」という挙動があり、原因は2つ別々だった。

**1. 幅を二重に数えていた。** 枠の幅を
`max(地域の幅, 地域の高さ) × 1.6 × アスペクト比` で出していた。
大きい方の辺を採ったうえで、さらに横長のアスペクト比（世界地図は 2.46）を
掛けている。横長の国では幅を2回数えることになる。

| | 旧 | 新 |
|---|---:|---:|
| 🇷🇺 ロシア | **0.51倍**（＝ズームアウト） | 1.55倍 |
| 🇨🇦 カナダ | **0.96倍** | 1.55倍 |
| 🇺🇸 アメリカ | **1.17倍** | **1.65倍** |
| 🇨🇳 中国 | 1.24倍 | 2.16倍 |
| ズームアウトする国 | 171中2 | **0** |

正しくは「地図のアスペクト比を保つ最小の枠」＝
`max(幅×余白, 高さ×余白×アスペクト比)`。倍率は 1.55〜3.5 に収める
（下限が無いと巨大な国で枠が地図を超え、上限が無いと小国が海岸線で埋まる）。

**2. bbox の中心が国の中心ではない。** 国の bbox はその国の領土すべてを
含む——アメリカならアラスカとハワイ、フランスならギアナとレユニオン、
ロシアなら日付変更線の向こう側。

| 国 | bbox の中心 | バブルのアンカー |
|---|---|---|
| 🇺🇸 アメリカ | (98, 77) ＝太平洋上 | **(128, 107)** |
| 🇫🇷 フランス | (329, 142) ＝本土より南 | 本土は約 (300, 105) |

地図が既にバブルを置くのに使っている座標に寄せる。探す人が見る場所は
そこだから。

### 世界地図は地球儀

平面の世界地図は 900×366、収める枠はほぼ正方形。どう嵌めても何かが余る
——地図を小さくして枠を空けるか、枠を低くして下の記事から場所を奪うか。
**球は正方形**なので、同じデータが同じ枠で数倍大きくなる。

常に半分は裏側にある。その代償は、効く場面で払い戻される：
**スポットライトの対象国を正面に回してからピンを打つ**ので、見せられて
いる場所が裏にあることは無い。

| | |
|---|---|
| 元データ | 正距円筒（`x=(経度+132)×3`, `y=(74−緯度)×3`）→ 経緯度に完全に逆変換できる |
| 投影 | 正射（遠方から見た球）。傾き22°N——赤道正面だと両極が隠れ、ドローン報道は北半球に多い |
| 点数 | 4,665点。各点を単位ベクトルで持つので、1フレームは1点あたり6回の乗算のみ（三角関数は回転そのものの2回だけ） |
| 描き直し | 0.35°以上回ったときだけ。171本の `<path>` は使い回し、`d` を書き換える |
| 自転 | **22秒で1周**（回っている間）。ピン表示中は止まり、世界地図の順番は14秒ごとに来るので**動いているのは半分の時間**——実質は44秒で1周、16°/秒、見えている面を横切るのに11秒 |
| 回る向き | **東向き＝画面上は左から右**。`project` は +x を画面右に送り、`toVector` は東経を +x に置くので、地図と同じく東が右。地球は東へ回るので、見えている面は右へ動く——それを起こすのは角度を**減らす**方 |
| 裏側 | 点ごとに切る。水平線をまたぐ形は、そこで線を切って手前だけ描く |
| タブ非表示時 | 止める |

**ピンの動き**：対象国を正面へ**早回しし、到着を待って**ピン → その点を
中心に球を1.55倍へ寄る → **記事が出ている間は静止** → 解放。

**回頭は必ず東回り。** 近い方の弧を取ると、少し後ろにある国へ向かうときに
一瞬逆回転する。逆回転する球は球ではなくダイヤルなので、自転と同じ向きに
統一した。10度「後ろ」の国へはほぼ1周かけて行く——それが素直な行き方で、
2秒弱で着く。

速度はイージングではなく**所要時間で決める**。イージングだと距離が速度に
なるので、近い国は弾かれるように動き、遠い国は最後がのろい。距離に応じて
0.5〜2.0秒に割り当てれば、どの距離でも同じ仕草に見える。

**リング・中心・アイコンは画面座標に1回置くだけで、球と一緒には動かない。**
だから、それらが出ている間は球を止める必要がある。ここで3つ間違えた。

1. **到着を固定待ち時間で決めていた。** 0.6秒固定で撃って「正面にあるか」を
   判定していたが、遠い国は間に合わず判定に落ち、**ピンが出ないまま**だった
   → 到着を監視する
2. **「正面に来たか」で撃っていた。** 水平線を越えた瞬間に成立するので、
   回頭の大半を残したまま撃っており、ピンは60度先の海の上に取り残された
   → 到着そのもの（`target` が消えたこと）を待つ。さらに**撃つ瞬間に
   残りの回頭を確定させる**ので、タブの間引きで数度残っても位置は正確
3. **止める仕掛けが無かった。** `target` は到着した瞬間に自分で null に
   なり、次のフレームから自転が再開する。`releaseGlobe` は既に null の
   ものを null にするだけで、何も止めていなかった
   → `spinning` フラグで止める。解放はピンを消すのと**同じtick**で
   （数百ミリ秒の隙間でも数度ぶん滑る）

拡大の中心は**箱に対する割合**で指定する。viewBox 単位（0〜400）の数値を
px として渡していたので、中心が別の場所になっていた。

**ピンはバブルとは別レイヤに置く。** バブルは球と一緒に動くので毎フレーム
作り直すが、その際 `innerHTML = ""` で層ごと消える。同じ層に置いたピンは
16ミリ秒で消えていた。

**キャプションの位置は viewBox から出す。** 元データは 900×366 だが描かれる
球は正方形。ファイルの縦横比で計算すると、平面地図の下端があった場所——
だいたい赤道——にキャプションが来て、球の下半分を覆った。

`facingRotation` は当初符号を逆にしていて、角度を打ち消すはずが倍にして
いた。日本の奥行きが0.25（本来0.97）、10か国中8か国が裏側に回っていた。

### 地図の列幅

日本の列は 228px（狭幅時 132px）の固定だった。日本列島は縦長なので、
列を広げても島の横に余白が増えるだけ——という理由づけは地図としては
正しいが、**その列はキャプション（その地域の記事）も載せている**。
228pxでは見出しが1行4語、132pxでは3文字になる。

地図に幅は要らないが、その下の文には要る。両地図を同じ 1fr にした。

| | 旧 | 新 |
|---|---:|---:|
| 日本パネル（1920幅） | 228px | **403px** |
| 世界パネル（1920幅） | 523px | **403px** |
| 日本パネル（1280幅） | 132px | **252px** |

日本の列を広げると地図自身も伸びようとする（369px幅なら420px高）ので、
帯の上限 344px で頭を打たせる。**幅はキャプションのためであって
地図のためではない**ので、譲るのは地図。

### 地図とキャプションの重なり

キャプションはパネル下端に固定した重ね置き。世界地図では問題にならない
——横長なので枠の上に寄って描かれ、キャプションは元々空いていた海の上に
載る。日本は縦長で枠を埋め切るので、**下3分の1が常に隠れた。九州と四国が
一度も画面に出ない。**

地図に天井を与える：パネルの高さから、キャプションの帯と枠の内側余白を
引いた分。天井はパネルから決めるので、地図自身の描画高から決めると起きる
「毎回少しずつ縮む」は起きない。

| | 変更前 | 変更後 |
|---|---:|---:|
| 日本地図に隠れていた高さ | 約100px | **0** |
| 日本地図の大きさ（1920幅） | 枠いっぱい | 162×184（全体が見える） |

縦長の地図を高さで縛る以上、幅は余る。南が無い地図は日本地図ではないので
これは妥当な交換だが、余白の使い道は別途検討の余地がある。

**キャプションの位置は地図ごとに決める。** 以前は両地図の高いほうに揃えて
いたので、横長で低く描かれる世界地図の下に空白の帯ができた。それぞれ自分の
地図の直下から始め、下端はパネルの底で揃う——見えるのは底の線の方。
結果、世界側のキャプションは96→178pxになり、空白が本文に変わった。

### 地図キャプションの置き場所

キャプション（地図の下に出る記事）は絶対配置で、`top` を JS が
「地図の自然な高さ + 12px」に設定していた。パネルは地図が欲しがる高さより
必ず高い、という前提の作り。

帯を302pxに縮めた時点でその前提が崩れた。地図が枠を埋め切り、キャプションの
`top` が枠の下端を越え、`top` と `bottom: 8px` の間で引き伸ばされる要素
なので**高さが負になって記事が消えた**。

`top` に上限を付けた。キャプションは常にパネル下端に84pxの帯を確保し、
譲るのは地図の側。

### 1件でも多く入れるための削り

1列化で1ペイン12件→6件に減った分を、周りから取り返した。

| | 旧 | 新 | 効果 |
|---|---:|---:|---|
| 地図・動画の帯 | 378px | **331px** | 動画枠を幅ではなく高さで縛る（16:9のまま中央寄せ） |
| 動画の次候補 | 2件 | **1件** | |
| 記事上の「すべて」行 | 28px | **0px** | 見出しと件数はサイドバーが既に出している。切替2つは上部バーへ |
| **1列の件数** | 6件 | **7件** |

一度302pxまで詰めて8件にしたが、地図と動画が窮屈だったので1件分だけ戻した。
帯は見るためのもので読むためのものではないから、一覧の1行と引き換えにする
価値はある——4行分ではなく。 |

「絞り込み中」の表示は解除ボタンが兼ねる（例：`規制・制度 26件 ✕`）。
消すなら押す場所に書いてある方がいい。

### ジャンル送りのチップ

**カテゴリ15個ではなくグループ7個**を並べる。15個をペイン見出しに詰めると
1個20pxになり、狙って押せない。サイドバーが7グループで整理されている以上、
ここだけ第2の分類体系を持つ理由も無い。7個・30×28pxに。

### レーダーの接点に触れる

輝点は直径2.6単位——画面上で約3px——のうえ脈動しているので、当てるのが
運任せだった。**当たり判定を別に置く。** 透明で面積5倍、脈動しない円を
輝点の上に重ね、そちらがポインタを受ける。

ツールチップも自前に置き換えた。ブラウザ標準の SVG `<title>` は表示まで
約1秒、1行、ポインタが1px動くと消える。48個の3px の接点に対しては
無いのと同じ。自前のものは即座に出て、種別・発信元・経過時間・見出し全文を
出す。SNS本文からURLを外し、4行で打ち切る（それ以上は指している盤面を
覆ってしまう）。

クリックで記事を開くのは以前から動いていたが、当たり判定が小さすぎて
届いていなかった。

### 表示切替ボタンの文言

記事一覧の見出しに2つ並んでいる。この2つが**別々の流儀**で書かれていた。

| | 旧 | 問題 |
|---|---|---|
| 地図の表示 | 「グラフを隠す」→「グラフを表示」 | 動詞。押すと何が起きるかを書いていた |
| 分類の切替 | 「分類ごと」⇄「時系列」 | 名詞。いまどちらの状態かを書いていた |

隣り合うボタンで文法が違うと、どちらの読み方をすればいいのか決められない。
さらに「グラフ」という語が誤りで、この行にあるのは日本地図・世界地図・
連続再生プレーヤーであってグラフは1つも無い。**地図を消した人が地図を
探しても、「グラフを表示」に目が行かない。**

両方とも「**操作対象の名前を書き、有効なとき点灯**」に統一した。

- 「● 地図・動画」——点灯＝表示中
- 「● 分類ごと」——点灯＝ジャンル別、消灯＝新着順の一覧

文言が状態で変わらないので、消したあとも同じ場所に同じ名前が残る。
戻し方を探す必要が無い。

### 地図のスポットライト（ピコーン）

7秒ごとに1地域を選び、ピンを打ってキャプションに記事を出す。選び方に
2つの欠陥があった。

**1. 同じ地域ばかり出る。** 「新着順に並べ、直近4回に出た地域は飛ばす」
という作りだった。1日に入る記事のうち **ロシア60件＋ウクライナ26件で
54%** を占めるので、4回の間隔は間隔にならない。5回に1回ロシアに戻り、
別の見出しで同じ戦争の話が出る——外から見れば「同じのばかり」。

履歴を伸ばしても解決しない。伸ばすと地域を使い切ってリセットし、
リセット先はまた同じ数地域になる。

そこで**地域の総当たり**に変えた。ニュースのある地域は、他の全地域が
一巡するまで2度目の順番が来ない。地域内では新着順に消費する。
ウクライナはニュースの量なりに出るが、1時間に4回は出なくなる。

一巡の目安：世界32地域で約3.7分、国内13地域で約1.5分。

**2. 昨日の記事が今日も出る。** 記事の保持は45日で、スポットライトは
その全部から選んでいた。最後のドローン記事が1週間前の地域は、順番が
来るたびにその1週間前の記事を出す。**48時間**より古い記事は選ばない
（対象地域が4未満なら7日、2未満なら14日まで広げる）。

GitHub のリポジトリも除外する。作者の所在で国が付いているだけで、
その土地について何も言っていない。

### 縦長のウィンドウ

幅1200px以下では、**地図2枚を横に並べたまま、動画を下段に回す**。

以前は1列に畳んでいたが、それは幅を捨てる配置だった。日本地図は縦横比が
0.88、地球儀は正方形なので、全幅のパネルは中央に小さな地図があるだけの
余白になり、しかもパネルは無駄に高い。

| | 旧（1050px幅） | 新 |
|---|---|---|
| 地図の配置 | 1列・全幅610px | **2列・各300px** |
| 球の直径 | — | 242px |
| キャプションの重なり | **75px** | **0px** |

境界を1100→1200pxにしたのは、その間だと3列が各206pxになり、地図にも
その下の文にも狭すぎるため。820px以下は分ける幅が無いので1列。

**地図パネルに最低高（380px）を与える。** 広い配置では帯の上限が高さを
決めているが、縦長配置には上限が無く、`height:100%` のSVGが内容で決まる
箱に入ると潰れる——166pxの箱に73pxの地球儀になっていた。

### 横送りは列の位置で送る

主要ニュースの横送りが `clientWidth` 分だけ動いていた。列は枠と同じ幅で
**8pxの隙間**があるので、毎回8px手前で止まり、誤差が積もって見出しが
左右に割れた状態で駐まる。列の実位置へ送るようにした。

折返しの判定も間違っていた。`max`（＝scrollLeftの最大値）は有効な位置で、
最終列はちょうどそこから始まる。それを行き過ぎと見なしていたので、
**どの回も1列目に戻り、実際には一度も送られていなかった。**

### 記事ペインの埋め方

左右2ペイン。列は上から詰めて、いっぱいになったら次の列を作る——これだと
**件数が少ないときに1列目だけに縦積みされ、右半分が丸ごと空く。**
検索で絞ると必ずこうなる。

列を埋める前に幅を埋める。必要な列数がペインの列数（2）に満たないときは、
2列に分配する（6件なら3+3、5件なら3+2）。1列しか無いときはその列を全幅に
伸ばす——半分の幅で半分が空では意味がない。

タイルは列の高さを分け合うが上限178px。上限が無いと2件のペインが
1件あたり画面の半分になり、強調ではなく不具合に見える。逆に上限が
きつすぎると、余白が列の横から列の下へ移るだけになる。

### レーダーに何を載せるか

盤面の定員は48。年齢順に詰めると**SNSが全枠を取る**。投稿は分単位で届き、
記事は書かれた時点で数時間前だから。ドローン**ニュース**の盤面が
ツイート48件でニュース0件になっていた。

そこで記事とSNSを別々に順位付けし、記事に55%を確保。余った枠は相手に回す。

ラベル（名前を書く5件）の選び方：

1. 記事が先、SNSは後
2. 記事の中では**新着順ではなく重要度順**。新しいことは中心に近いことで
   既に示している。ラベルが足すべきなのは「どれが効いているか」
3. **見出しが識別子のものは下げる。** collector は GitHub を一律12点と
   採点するが、これは大半のニュースより高い。点数は間違っていない
   （リリースは重要）——「タイトルが文かどうか」を測っていないだけ。
   `Yarindos/drone_in_sky` は名前であって、名前は @ハンドルと同じくらい
   何も言わない。輝点としては残り、ツールチップも持つ
4. SNSは本文から@メンション連鎖・URL・ハッシュタグ・絵文字を落とした
   残りを使う。アカウント名では「8人が投稿した」以上のことを言わない

### レーダーの文字

ジャンル名は円の外に置く。内側にあった頃は自分のセクターの輝点と、
毎分4回通るサーチビームの上に重なっていた。

外に出すために viewBox を 200×200 から 268×200 に広げている。パネルは
円より横に広く、正方形の viewBox では左右に36pxずつ余っていた。高さは
変えていないので、円の直径は 239px→229px とほとんど縮んでいない。

ジャンル名は「・」で切って前半だけを出す（防衛・軍事→防衛、開発・OSS→
開発）。文字数で切ると「開発・O」「ビジネス・」になり、描画の失敗に見える。

記事の見出しは句読点で切る。ただし**予算の6割以上を使う位置に限る**。
最初の句点で切ると「Amazon、ドローン配送を強化、注文数100万件を目指す」が
「Amazon」になる——正しい節ではあるが、ニュースではない。

ラベルの幅は全角・半角を数えて見積もる。1文字5.6と一律に置いていたため、
8文字のつもりが20文字通って盤面からはみ出していた。

## 8.5 新着の表し方

新着は複数の場所で同時に示す。1か所だと見落とすため。

| 場所 | 表現 |
|---|---|
| 更新ボタン | じわーっと点滅 |
| サイドバー | 該当グループ行が明滅 |
| 記事カード・タイル | 枠が明滅 |
| **地図** | **記事が届いた地域が明滅** |
| レーダー | 輝点が脈打ち、外向きに広がるリングを伴う |
| カレンダー | 該当日のマーカーが明滅 |

**色はそのジャンルの色。** 単一の赤は「何か来た」しか言わない。ダッシュ
ボードが既にそのジャンルに使っている色なら「何が来たか」まで言える。
同じ記事が光らせるサイドバー行・タイル・地図・輝点が同じ色で脈打つので、
どこを見ても同じ出来事だと分かる（要素ごとに `--fresh` を設定）。

**地図には新着の表現が無かった。** 更新ボタン、サイドバー、タイル、
レーダーは光るのに地図だけ黙っていたので、地図が取りこぼしたように見えた。

### 何を新着とするか

届いたもの全部が「到着」ではない。**GitHub・動画・コミュニティは除外**する。

- GitHub は一日中リポジトリを push する
- SNS は20秒ごとに入れ替わる
- 動画はプレイヤーが延々と消化していくもので、1本増えるのは事件ではない

これらを新着に数えると、**読者がニュースと呼ばないものに対して音が鳴り**、
しかも光らせる先が記事ペインに出ない種類の項目なので「音はするのに
何も光らない」状態になっていた。

レーダーのリングは、盤上で唯一「外へ動くもの」なので、48個の輝点の中から
探さなくても目に入る。点が少し大きく少し赤いだけでは、探している時にしか
気づけない。

## 8.6 急上昇（サイドバー下段）

他のパネルは全部「何があるか」を答えている。ジャンル別の件数、地域別、
新着順。「今日は何が違うか」を答えるものが無かった。普段1日2件の話題が
今朝11件来ている——3000件の中の11件は、既存のどのランキングにも出ない。

直近24時間の出現数を、その前6日間の1日あたり平均と比べ、比率で並べる。

- 抽出は3種類だけ：ラテン語（DJI, Skydio, BVLOS）、カタカナ3字以上、
  漢字2〜6字。ひらがなは文法であって主題ではないので取らない
- 1記事につき1回。同じ記事内での連呼で上がらない
- 2字の漢語は一般語が多いので、比率のしきいを 1.6 → 3 に上げる
- **同じ記事群から出た語はまとめる。** 「アマゾン、軽量荷物の配送を都市部へ
  拡大予定」は アマゾン／軽量荷物／都市／拡大予定 が同時に跳ねる。記事集合の
  重なりが6割を超えたら、比率の高い方だけ残す
- 表示件数は枠の高さから決める。`overflow: hidden` に7行を流し込むと、
  「上昇率の高い順」の下2行が黙って消える
- **GitHub と動画は数えない。** 一日中 push され、動画検索は延々と補充
  されるので、「上昇」を測る仕掛けはそれを上昇と読む。新着の鐘から外すのと
  同じ理由。加えて GitHub のタイトルは `owner/repo — 説明` なので、
  **識別子の部分を落としてから**語を切る——ある1アカウントが54個の
  リポジトリを登録していて、`api-evangelist` がその日最大の話題として
  上がっていた
- **2つ以上の別の話題に出ること。** 件数は記事単位で、1つの出来事は
  十数本の記事としてここに届く。だから1本のよく報じられた記事から
  切り出された断片が、12回出現したように見えた。媒体数では分かれない
  （Google News は同じ出来事を多媒体から集めるのが仕事）。**話題**を
  数えると分かれる。collector が重複報道をまとめて `related` を付けて
  いるので、その組の代表IDで数える
- 漢字語は「ヶ・ヵ・ノ」を含めて切る。含めないと 青木ヶ原樹海 が
  「青木」になり、地名が人名に見える

### 収集状況の扱い

ソース91本を1本1セルで並べるパネルを一度作ったが、90個のセルが
ほぼ常に「正常」と言うためにサイドバーを使っていた。不調の件数を
「ソース管理」の行に出すだけにして、枠は急上昇に譲った。

止まったフィードは「その週ニュースが無かったフィード」と見分けがつかない
ので、件数自体は出す価値がある。「87/91」ではなく「7件不調」と書くのは、
引き算をさせないため。

## 8.7 web を5つ目のOSとして

同じ `droneradar/web/` が2つの形で動く。`js/mode.js` が起動時に一度だけ
`/api/status` を叩いて、どちらに話しかけているかを決める。ホスト名では
判断しない——Mac 版はループバック・Bonjour名・LANアドレスと複数の名前で
到達し、公開先も動きうるので、名前で書いた規則は最初にどちらかが動いた
日に黙って壊れる。404 は曖昧でない。判定不能なら静的側に倒す
（読めるが書けない、の方が安全側）。

| | 公開版 | Mac 版 |
|---|---|---|
| 収集 | Actions が毎時07分 | アプリ起動中に15分ごと |
| 読み | `api/*.json` | `/api/*` を都度生成 |
| 保存済み | localStorage（端末ごと） | サーバ側 |
| ソース追加・手動収集 | **不可** | 可 |
| 画像 | 発行元へ直リンク | `/img?u=` プロキシ |

### web で実現できないこと

1. **書き込み全般**（ソース管理・手動収集・設定）。静的配信に POST の
   相手はいない。押すと「この画面は読むだけです」と出して、黙って
   失敗させない
2. **Reddit 系3ソース**。Reddit はデータセンターIPに 429 を返し、
   Google News も Reddit を索引していないので代替経路が無い
3. **保存済み記事が端末ごと**。45日の保持期限からも守れない

### 同等だったもの（実測）

疑っていたが公開版で成立していた：記事4,835件、英語記事の**81%が和訳**、
重複のまとめ99クラスタ、SNS 220件、株価12/12、`http://` 画像0件。

### インストールできる形にする

`app.webmanifest` とアイコン（192/512）をビルド時に生成する。他の4ビルドは
自前のウィンドウとアイコンを持つのに web だけブラウザのタブで、**5つの中で
読者が実際に気づく差はそこだった**。iOS は manifest の display を読まないので
`apple-mobile-web-app-*` も併せて置く。

manifest を `web/` に置かずビルド時に書くのは、`start_url` と `scope` が
公開先からの相対で決まる一方、Mac 版は同じディレクトリを別の根から出すため。

## 9. リモコン操作

Fire TV には十字キー・決定・戻るしかない。1段階で扱うと60件並ぶ行が破綻するので2段階にした。**全OSで同じ挙動**、キーボードの矢印キー・Enter・Escape も同一コードで動くので、デスクトップがそのまま検証台になる。

- **第1段階** — 左右でツール間を移動（サイドバー→掲示板→日本地図→世界地図→動画→記事→カレンダー→レーダー→SNS→株価）
- **第2段階** — 決定で入り、上下左右で項目間を移動、決定で開く、戻るでツールへ、もう一度戻るで解除

上下は添字送りではなく画面上の行で判定する。記事欄は5段の列組みなので、添字だと「下」で横に飛ぶ。空のパネルは飛ばし、行の端で左右に出ると隣のパネルへ移る。ポインタを触ると解除される。

## 10. 見送った案（2026-08-19）

**サーバを立てて Web から見られるようにする案は採らない。**

得られるのは「Mac を止めたまま外出先から見られる」の一点で、代わりに次を抱える。

- **認証の実装が必須。** 書き込みAPI（収集実行・ソース追加・キーワード追加・
  ブックマーク・設定変更）は認証がゼロで、127.0.0.1 限定を前提に作ってある。
  公開すれば URL を知った誰でも設定を書き換えられる
- **運用が発生する。** OSの更新、証明書、落ちたときの対応。いまは
  アイコンをダブルクリックで完結している
- **スクレイピングが通るか不明。** YouTube検索6本と Yahoo!リアルタイム7本は
  ページを読んでいるので、データセンターIPからは遮断される可能性がある。
  落ちれば動画欄とSNS実況欄が動かなくなる

Google Drive は配信層になれない。ホスティングは2016年に廃止され、直リンクは
CORS ヘッダを返さないのでWebページから読むこともできない。保管庫にはなるが、
Drive 自体は計算機ではないので収集はできない。

同一Wi-Fi内で別端末に映すだけなら `DRONERADAR_LAN=1` で足りる（7章）。

## 11. 既知の制約

- Google News 経由の記事は URL が難読化されており、画像が取れない。媒体名の
  代替タイルで埋め、同一話題の他媒体から画像を借りる
- 日本地図に載るのは全体の約1割。全国紙の記事は地名を含まないことが多い
- 動画は埋め込み拒否や地域制限のものがあり、9秒の停止検出で次へ送る
- macOS 版は未署名。Windows 版は Windows での動作未検証
- モバイル2種は実機で未検証。ビルドと同梱内容は確認済みだが、Capacitor の HTTP
  プラグインが実際にフィードを取れるか、リモコンが Fire TV の実キーコードで
  動くかは端末に入れるまで分からない
- iOS は未署名。実機に入れるには Apple Developer Program か SideStore が要る

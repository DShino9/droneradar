# DroneRadar 仕様書

ドローン関連情報を一画面に集約するダッシュボード。macOS / Windows / Android / iOS / Fire で動作し、
外部ライブラリと API キーを一切使わない。
動かし方は [README.md](README.md)。

- 最終更新: 2026-08-19
- 収集ソース: 106本 / 分類: 17ジャンル（7グループ）
- コード規模: Python 2,946行 + JavaScript 3,801行（UI 2,683 / 共通 1,118）+ CSS 1,145行

**この文書は入口だけを持つ。** 中身は「壊れたとき開く1冊」ごとの分冊にある（§3 の表）。
2026-09-26 に旧 `SPEC.md`（979行）をこの入口と分冊に割った。本文は1字も書き換えていない
（数値・行数・本数も当時のまま。食い違いがあっても直していない）。

**読む順**：改修の前に読むのは「触る分冊」1冊と [課題/一覧.md](課題/一覧.md) だけでよい。

## 1. 満たすもの

1. ドローン関連情報を一画面に集約する。スクロールしない前提でレイアウトを組む
2. 外部ライブラリと API キーを使わない。Python は標準ライブラリのみ、フロントもフレームワークなし
3. Mac 版は既定で 127.0.0.1 のみに立ち、外部にデータを送らない（`DRONERADAR_LAN=1` のときだけ同一 Wi-Fi に開く）
4. 常に最新を出す。Mac 版は15分ごと、公開版は Actions が毎時07分に収集する。SNS は60秒、株価は5分
5. 同じ `droneradar/web/` が **Mac 版（書ける）と公開版（読むだけ）** の2つの形で動く
6. macOS / Windows / Android・Fire / iOS の配布物を `tools_build_all.sh` で作れる
   （実機で確かめていない端末がある → §6）

## 2. 置き場と動かし方

| | |
|---|---|
| 一式 | `~/claude code/DroneRadar/`（別の git。`github.com/DShino9/droneradar`、main） |
| Mac 版 | `/Applications/DroneRadar.app`（`tools_install.sh`）。127.0.0.1:8783 にサーバを立て、Chromium のアプリモードで開く |
| 公開版 | GitHub Actions `collect`（毎時07分）が収集し、data ブランチに積み、静的に配る |
| 収集データ | `data/`（リポジトリには置かない。data ブランチが持つ） |

手順は [README.md](README.md)。

## 3. 作りの骨

### 全体構成

```
[収集]  Python                    [表示]  ブラウザ
  scrapers ── RSS/HTML/JSON          js/api ──┐
      ↓                                       │
  collector ── 正規化・分類・地理判定 ──→ items.json ──→ 各描画モジュール
      ↓                                       │
  translate ── 日本語訳（キャッシュ付）       └── 15の ES モジュール
      ↓
  server ── HTTP + 静的配信
```

サーバは 127.0.0.1:8783 に立ち、`.app`（または `.bat`）がそれを起動して
Chromium のアプリモードでウィンドウを開く。UI は HTML/CSS/JS なので、
実体は「ローカル Web アプリを OS のアプリで包んだもの」。

**外から見て分からない依存**：収集エンジンが2実装ある（Python ＝デスクトップ、`core/` の JavaScript ＝モバイル）。
片方だけ直すと食い違う。公開版は `js/mode.js` が起動時に `/api/status` を叩いて Mac 版か静的かを決める。

### 分冊（壊れたとき開く1冊）

| 分冊 | 受け持ち | 開くのはこんな時 |
|---|---|---|
| [収集とサーバ](仕様/構成要素/収集とサーバ.md) | Python モジュール、ソースの種別、item、ジャンル判定、予備経路、ソースの穴、HTTP API | ソースが不調／記事が来ない／分類がおかしい |
| [画面の骨組み](仕様/構成要素/画面の骨組み.md) | フロント19モジュール、依存の向き、ジャンルの色とアイコン、国旗、動画、リモコン | 循環 import／色が違う／リモコン／動画が止まる |
| [記事ペインと掲示板](仕様/構成要素/記事ペインと掲示板.md) | 電光掲示板、記事ペイン、ジャンル送り、ボタンの文言、横送り、埋め方 | 同じ見出しが回る／片側が空く／横送りがずれる |
| [地図のスポットライト](仕様/構成要素/地図のスポットライト.md) | 地域の総当たり、全国、世界地図の日本、ピンとバブルのアイコン | 同じ地域ばかり／古い記事／全国が出ない |
| [地図の配置とキャプション](仕様/構成要素/地図の配置とキャプション.md) | キャプションの高さと位置、列幅、地図の天井、縦長ウィンドウ | キャプションが切れる・消える／地図の下が隠れる |
| [地球儀](仕様/構成要素/地球儀.md) | 世界地図の球、自転と回頭、ピンの時機、ズーム | ピンが出ない／逆回転／ズームアウト |
| [レーダー](仕様/構成要素/レーダー.md) | 盤面の定員と配分、ラベル、当たり判定 | 盤面が英語・SNSばかり／触れない |
| [新着と急上昇](仕様/構成要素/新着と急上昇.md) | 新着の表し方、何を新着とするか、急上昇、収集状況 | 鳴るのに光らない／急上昇に変な語 |
| [配布と端末](仕様/構成要素/配布と端末.md) | ビルドと配布、4プラットフォーム、`core/`、web（公開版）、manifest | ビルド／端末で動かない／公開版だけおかしい |

決定の記録は [仕様/決定/](仕様/決定/)（覆したくなった時だけ読む）。

## 4. 決めたこと（なぜそうしたか）

### 設計の前提

制約は「無料の範囲で」の一点から出ている。これが全ての設計判断を規定した。

| 前提 | 帰結 |
|---|---|
| API キーを使わない | X は Yahoo!リアルタイム検索の `__NEXT_DATA__`、YouTube は検索結果の `ytInitialData` から取る |
| 外部ライブラリを使わない | Python は標準ライブラリのみ。フロントもフレームワークなし |
| 常に最新を表示 | 15分ごとに自動収集。SNS は60秒、株価は5分 |
| 一画面に収める | スクロールしない前提でレイアウトを組む |
| ローカルで完結 | 既定で 127.0.0.1 のみ。外部にデータを送らない |

### ほかの決定（本文は分冊・ADR にある）

- サーバを立てて Web から見られるようにする案は採らない（2026-08-19）→ [ADR-0001](仕様/決定/ADR-0001-サーバを立ててWebから見る案は採らない.md)
- 拒まれるソースは置き換えではなく**予備**として Google News へ回す。検索経由は `via="search"` を残す → [収集とサーバ](仕様/構成要素/収集とサーバ.md)
- 分類は17のまま、色は7グループが持つ → [画面の骨組み](仕様/構成要素/画面の骨組み.md)
- 記事IDは同期の FNV-1a 系、切り詰めは文字単位、実体参照はセミコロン省略に対応（移植の判断3つ）→ [配布と端末](仕様/構成要素/配布と端末.md)
- 公開版か Mac 版かはホスト名ではなく `/api/status` で決め、判定不能なら静的側に倒す → [配布と端末](仕様/構成要素/配布と端末.md)
- 地図のスポットライトは地域の総当たり、48時間より古い記事は選ばない → [地図のスポットライト](仕様/構成要素/地図のスポットライト.md)
- 世界地図は地球儀。回頭は必ず東回り、速度は所要時間で決める → [地球儀](仕様/構成要素/地球儀.md)
- レーダーは記事に55%を確保、識別子の見出しはラベルから外す → [レーダー](仕様/構成要素/レーダー.md)
- GitHub・動画・コミュニティは新着にも急上昇にも数えない → [新着と急上昇](仕様/構成要素/新着と急上昇.md)
- 記事ペインは幅を優先（Yahoo!ニュース準拠の1列625px・太さ400）→ [記事ペインと掲示板](仕様/構成要素/記事ペインと掲示板.md)

## 5. 触らないもの・踏んだ地雷

各分冊の末尾「地雷（踏んだもの）」が正本。ここは索引。

- **2026-08-30** `merge_builtins` の項目一覧に `fallback` を入れ忘れ、データが一世代前のまま公開した。`core/collector.js` の `OWNED` も同じ穴 → [収集とサーバ](仕様/構成要素/収集とサーバ.md)
- 静的配信は `realpath` の**後で**公開フォルダ配下か検証する → [収集とサーバ](仕様/構成要素/収集とサーバ.md)
- 葉のモジュールから `main` を import しない（`api → cards → api` の循環が実際に起きた）→ [画面の骨組み](仕様/構成要素/画面の骨組み.md)
- キャプションの `top` に上限が無いと高さが負になって記事が消える → [地図の配置とキャプション](仕様/構成要素/地図の配置とキャプション.md)
- ピンをバブルと同じ層に置くと `innerHTML = ""` で消える。`facingRotation` の符号 → [地球儀](仕様/構成要素/地球儀.md)
- 横送りは `clientWidth` ではなく列の実位置で送る → [記事ペインと掲示板](仕様/構成要素/記事ペインと掲示板.md)

日付の無いものは初版コミット（2026-08-30）にまとめて入っていた分で、正確な日は記録が無い。

### 既知の制約

宿題ではなく、受け入れて手当て済みのもの。

- Google News 経由の記事は URL が難読化されており、画像が取れない。媒体名の
  代替タイルで埋め、同一話題の他媒体から画像を借りる
- 日本地図に載るのは全体の約1割。全国紙の記事は地名を含まないことが多い
- 動画は埋め込み拒否や地域制限のものがあり、9秒の停止検出で次へ送る
- macOS 版は未署名。Windows 版は Windows での動作未検証
- Reddit 系3ソースは公開版では取れない（代替経路が無い）→ [配布と端末](仕様/構成要素/配布と端末.md)

## 6. 未達・宿題

→ [課題/一覧.md](課題/一覧.md)（生成物）。改修の前に読む

旧 `SPEC.md` 11章「既知の制約」のうち、まだ確かめていないもの（Windows 実機・モバイル実機・iOS の署名）は課題に移した。

## 7. 変更の作法

- **収集の作りを変えたら、Python（`droneradar/`）と JavaScript（`core/`）の両方に入れる。** `core/` は Python と照合して 100% 一致を確かめてある（[配布と端末](仕様/構成要素/配布と端末.md) の表）
- ソース定義に項目を足したら `merge_builtins`（Python）と `OWNED`（`core/collector.js`）の一覧にも足す
- リモコン操作はキーボードの矢印・Enter・Escape と同一コードなので、デスクトップで確かめられる
- `main` への push は Actions の収集を起こす。`README.md`・`仕様書.md`・`仕様/`・`課題/`・`SPEC.md` だけの変更は起こさない（`collect.yml` の `paths-ignore`）
- 作りを変えたら同じ作業でこの仕様書と分冊を直す（作業場 CLAUDE.md 6条）

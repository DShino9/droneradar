---
番号: 6
道具: DroneRadar
種類: 手入れ
状態: 済
大きさ: S
関連: [仕様書, 構成要素/収集とサーバ, 構成要素/画面の骨組み, 構成要素/配布と端末, 構成要素/記事ペインと掲示板, 構成要素/地図の配置とキャプション]
作った: 2026-09-26
更新: 2026-09-26
---

# 書類の数字が食い違っている

SPEC.md を割ったとき（#1）、原文の中で数字が食い違っていた：ソース 87本／106本、
フロントのモジュール 15／19、サーバ 462行／482行。実物で数えて、書類を実物に合わせる。

## こうなってほしい（受け入れ条件）

- [x] ソースの本数・フロントのモジュール数・サーバの行数を実物で数え、書類を直した
- [x] 書類の中の他の数字（本数・件数・間隔など）も、実物で照合できるものは照合して下に一覧にした
- [x] 直した数字の後ろに「（2026-09-26 数えた）」と数え方を書いた。確かめられないものは「未確認」と書いた
- [x] 今の収集結果で GitHub の IP から弾かれているソースを数えた

## 数え方

- コード：`wc -l`（Python `droneradar/*.py`、UI `droneradar/web/js/*.js`、共通 `core/*.js`、CSS `droneradar/web/style.css`）
- 設定：`droneradar/sources.py` を import して `DEFAULT_SOURCES` などの要素数。`tables/*.json` も同数か照らした
- 定数：JS／Python の定数を原文で読んだ（`ledboard.js` の `LED_POOL` など）
- 最新の収集結果：data ブランチ `収集 2026-09-26 01:29Z` の `sources.json`・`items.json`・`social.json`・`stocks.json`
  （手元の `data/` は 2026-09-01 で古いので使っていない）

## 照合の一覧（2026-09-26 数えた）

### 食い違っていて直したもの

| どこ | 書類 | 実物 |
|---|---|---|
| 仕様書・配布と端末 | ソース 87本（配布と端末の図） | **106本**（仕様書の106本が正しい） |
| 仕様書・画面の骨組み・配布と端末 | フロント 15／19モジュール | **20**（`mode.js` が表から落ちていた。表に足した） |
| 収集とサーバ・配布と端末 | server.py 482行／462行 | **468行** |
| 仕様書 | Python 2,946行 | **3,138行** |
| 仕様書 | JavaScript 3,801行（UI 2,683 / 共通 1,118） | **6,235行（UI 5,052 / 共通 1,183）** |
| 仕様書 | CSS 1,145行 | **1,792行** |
| 収集とサーバ | collector.py 847 / sources.py 508 | **885 / 512**。`snapshot.py`（65行）が表から落ちていた。表に足した |
| 画面の骨組み | 各モジュールの行数 | 12本が変わっていた（例 `maps.js` 334→960、`radar.js` 329→620、`api.js` 84→166） |
| 配布と端末 | core/collector.js 129行・合計 1,118行 / 44 KB | **194行・1,183行 / 42 KB** |
| 配布と端末 | web/ 356 KB・tables/ 48 KB | **530 KB・50 KB**（`git ls-files` のバイト数） |
| 仕様書・収集とサーバ・配布と端末 | Reddit 系3ソース | **定義は5本。最新の回は4本が 429、r/drones は通った** |
| 記事ペインと掲示板 | 地図・動画の帯 331px | **344px**（`.graphics { max-height }`） |
| 地図の配置とキャプション | 縦長で地図パネルに最低高 380px | **無い**。8d4eb6b（2026-08-31）で消え、1200px以下も帯は1段・`min(344px, 38vh)`。節の頭に注記した |
| README | スポットライト 約4秒 | **7秒**（`SPOTLIGHT_MS`） |
| README | 掲示板 24時間・候補60件・18件の窓 | **12時間（→24時間→3日）・36件・12件**（`ledboard.js`） |
| README | 掲示板の対象外 開発・研究・コミュニティ | **研究・開発・コミュニティ・イベント・動画**（`NEWSY` に無い5分類） |
| README | ジャンル自動送り 8秒 | **24秒**（`GENRE_ROTATE_MS`） |
| README | レーダーの見出し 新しい3件 | **重要度の高い5件**（`LABEL_MAX`） |
| README | Reddit の4本 | 定義5本・最新の回は4本が落ちた、に直した |

### 実物と合っていたもの

分類17・7グループと色、`fallback` を持つソース5本（5社）、`strict` 付き15本、SNS 14本、株価12銘柄、
語彙367語（12語群）、都道府県47・国75、`/api/*` 10本＋`/img`、背景ループ3本、
記事15分・SNS 60秒・株価5分、Actions 毎時07分、保存の上限5000件・45日、翻訳の上限400件、
レーダーの定員48・記事55%・見出し5件、掲示板 半減6時間・窓12時間・プール36・12件・3分、
ジャンル送り24秒・横送り20秒、スポットライト7秒・48時間（4地域未満で7日、2未満で14日）、
世界地図の順番14秒ごと、地球儀 22秒で1周・0.35°・171本の path・回頭0.5〜2.0秒、
動画の停止検出9秒・終了2.5秒、急上昇のしきい 1.6／2字は3、帯の上限344px、境界 1200px／820px。

### 未確認で残したもの

- core の照合件数と 100%（照合の台がリポジトリに無い）
- 配布物の大きさ4つと APK の内訳（ビルドし直していない）
- 画面を描かないと測れない px（1列の件数7件、帯を302pxに詰めた話—`style.css` の覚え書きは306px、列幅 403px/252px など）
- 記事ペインの「カテゴリ15個」がどれを数えたか（定義は17）
- 公開版の実測「99クラスタ」（当時の数え方が分からない）
- README のズームの間合い（約1秒後・1.5秒）、レーダーの「直近6時間」（窓は件数で30分〜12時間から選ぶ作り）

### 当時の実測として残したもの（変わる値。食い違いではない）

日本の記事435件・都道府県付き312件、ロシア438件、世界32地域・国内13地域（最新の回の48時間内では 28／19）、
公開版の記事4,835件・和訳81%（最新の回は 5,000件・3,505件中3,151件＝90%。書類に並べて書いた）、
ソースの穴の「87本の時点」、急上昇の「91本のパネル」、README の「1121pxで74件」。

## GitHub の IP から弾かれているソース

覚え書きは「9件」。最新の回（2026-09-26 01:29Z）で数えると **8本**：
直リンクが落ちて Google News の予備に回ったもの4本（dronetribune・dronelife・suasnews・dronegirl）＋
予備が無く HTTP 429 のままの Reddit 4本（reddit_search・reddit_fpv・reddit_dronephoto・reddit_fpvracing）。
verticalmag（予備あり）と r/drones はこの回は直リンクで通った。回によって揺れる。

## 動き

- 2026-09-26 立てた（#1 で割ったときの食い違いから）。状態 作業中
- 2026-09-26 実物で数えて 仕様書・分冊5冊・README を直した。照合の一覧をこのカードに置いた。状態 作業中 → 済
